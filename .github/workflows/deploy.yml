name: Deploy

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build and Push Images
        run: |
          echo "Building Docker images... (Placeholder)"
          # docker build -t mediguardian/backend:latest ./backend
          # docker push ${{ steps.login-ecr.outputs.registry }}/mediguardian/backend:latest

      - name: Update EKS Cluster
        run: |
          echo "Deploying to EKS... (Placeholder)"
          # aws eks update-kubeconfig --region us-east-1 --name mediguardian-cluster
          # kubectl apply -f infra/k8s/
